{
  "name": "shopping-server",
  "version": "1.0.0",
  "description": "Backend for shopping client",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-dev": "cross-env NODE_ENV=development nodemon node server.js",
    "start-prod": "cross-env NODE_ENV=production nodemon node server.js"
  },
  "dependencies": {
    "express": "^4.16.4",
    "isomorphic-fetch": "^2.2.1",
    "nodemon": "^1.18.6"
  },
  "devDependencies": {
    "cross-env": "^5.2.0"
  }
}
